const api = {
    login: async (username, password) => {
        console.log(username, password);
    },
    register: async (username, password) => {
        console.log(username, password);
    }
}
export default api;